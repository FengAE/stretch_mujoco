# Office snack assets

The mesh and texture files in this directory are copied from the bundled
`third_party/robosuite/robosuite/models/assets` object library. They are kept
next to the Stretch model so `office_scene.xml` also works when the Python
package is installed without the `third_party` source tree.

The source project and its license are available in `third_party/robosuite`.
